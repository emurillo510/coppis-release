<%= form_for [@post, Comment.new] do |f| %>
  
  <div class="comment-field">
    <%= f.text_area :content, placeholder: "Join the conversation", class: "form-control" %>
  </div>
  
  <div class="actions">
    <%= f.submit "Add comment", class: "btn btn-success" %>
  </div>
<% end %>

